Процедура ПриИзмененииРеквизита (Форма,Объект,ИмяТЧ,Элемент) Экспорт
	ИмяЭлемента  = Элемент.Имя;
	ИмяРеквизита = Лев(ИмяЭлемента,Найти(ИмяЭлемента,"_") - 1);
	УИД = Прав(ИмяЭлемента,СтрДлина(ИмяЭлемента) - Найти(ИмяЭлемента,"_"));
	
	ИскомыеСтроки = Объект[ИмяТЧ].НайтиСтроки(Новый Структура("УИД",УИД));
	Для Каждого ИскомаяСтрока Из ИскомыеСтроки Цикл
		ИскомаяСтрока[ИмяРеквизита] = Форма[ИмяЭлемента];
	КонецЦикла;	
КонецПроцедуры	
